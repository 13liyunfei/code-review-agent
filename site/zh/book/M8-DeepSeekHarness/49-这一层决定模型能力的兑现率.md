# 第 49 讲 · 这一层决定模型能力的兑现率

> 🎯 导读问题：**"同一个模型、同一份提示词，换一个 harness，表现为什么差一个档？"** ——能答出"模型给的是**能力上限**，harness 决定这份能力被**兑现多少**；所以'模型不行'是最省事、也最没信息量的归因"——这一句就把你和"把一切归因于模型"的人分开了。**本讲要建立的判据是：换 harness 就是换兑现率，而兑现率是**可设计**的——`Profile` 定组装、`Bundle` 定可被 patch 的边界、插件树定最终装配。**

> **模块八 · 进入方式**：前七讲你把「模型调用 / 上下文压缩 / 工具 / 权限 / 记忆 / 技能」六大机制**在自家 Java 系统里读了一遍**（43–48 讲）。这一模块换一个坐标：一个**真实开源 harness 的内部实现**——**DeepSeek Harness**（`dsh`）。它不教你"怎么调 API"，而是回答一个更值钱的问题：**当"六大机制"被做成一个可组合系统时，它们各自被放在了哪一层、用什么原语表达、替换一处会不会牵动全身。**
>
> **本模块面向算法 / 模型研究员，读法有三处不同（请先接受这三条）**：
> ① **代码是 TypeScript 与 Cordis 插件，不是 Java。** 引用的锚点是**真实文件路径与接口名**，不做逐行 Java 对照——你不必会写 TS，只需要能读"谁声明、谁实现、谁消费"。
> ② **重心在"为什么这样分层"**（可组合、可替换、可回放），不在"怎么调 API"。每一讲的判据句都是**架构判据**，不是用法。
> ③ **动手任务是"跑起来 + 改一个插件"**，不是改业务代码。
>
> **⚠️ 材料基准与诚实声明**：本模块所有路径、行号与引文，取自 **2026-09-26 实读**的 `github.com/deepseek-ai/deepseek-harness`，commit `477b4f4`（对应标签 `rel/dsh-0.1.7-rc.2`）。`dsh` 官方**自称处于「开发者预览」阶段并明确预告"未来将出现破坏兼容性的变更"**（`README.zh.md:11-13`）——所以**本模块引的文件路径是"这一版的事实"，不是"永久契约"**。凡是引用原文，都给出文件与行号；凡是本模块作者的推论，都会标成"推论"，**不冒充官方口径**。

<img class="mermaid-svg" src="/zh/book-assets/diag-0109.svg" alt="⚠️ 材料基准与诚实声明：本模块所有路径、行号与引文，取自 2026-09-26 实读的 `github.com/deepseek-ai/deepseek-harness`，commit `477b4f4`（对应标签 `rel/dsh-0.1.7-rc.2`）。`dsh` 官方自称处于「开发者预览」阶段并明确预告&quot;未来将出现破坏兼容性的变更&quot;（`README.zh.md:11-13`）——所以本模块引的文件路径是&quot;这一版的事实&quot;，不是&quot;永久契约&quot;。凡是引用原文，都给出文件与行号；凡是本模块作者的推论，都会标成&quot;推论&quot;，不冒充官方口径。" />

> **图 49-0**　本讲地图：换 harness 差的是**兑现率**，不是模型能力。`dsh` 把"兑现率"落在三层上——`Profile`（具名组装）、`Bundle`（可被上层 patch 的分发格式）、插件树（启动时按序叠加）。三层的共同效果是：**改行为不必改内核**。

## 一、痛点

你在模型侧做过这样的事：换一个 checkpoint、调一次 temperature、加两轮 SFT。然后你发现——**同一份权重，接进不同的 harness，表现能差出一个档**。同一个模型，在 A 里能稳定完成 12 步的工具调用，在 B 里第 3 步就开始丢掉前面的约束。

这时最省事的句式是"**模型不行**"。它的问题是：**它无法被证伪，也无法被改进。** 你说不出要动模型的哪一部分才能让第 3 步不丢约束——因为丢约束的那段逻辑，很可能根本不在模型里。

这里有一个被普遍低估的分工：

> **模型提供的是"给定上文、输出分布"这一件事的能力上限；而"上文到底是什么"、"工具能不能调"、"调错了谁拦着"、"这一步该不该继续"——全部由 harness 决定。**

所以"换 harness 差一个档"不是玄学，它有一个可测量的解释：**模型的上限没变，兑现率变了。**

如果你只做模型侧的研究，这条结论对你尤其重要：**你的实验里有一个不受控的变量，叫 harness。** 论文里写"我们的方法在 agent 任务上提升 8%"时，如果没交代 harness，那 8% 里有多少来自模型、多少来自"你把工具描述写清楚了"，是说不清的。反过来，**harness 也是你的杠杆**：不改一个权重，只把"模型能看见什么"表达对，就能把那 8% 拿回来一大半。

这一讲要回答的是最上层的一个问题：**一个把六大机制都做成可替换的系统，是怎么被"组装"出来的？**

## 二、原理

### 2.1 `dsh` 是什么：一个把"一切皆插件"当架构原语的 harness

先看它自己的自我定位（`README.zh.md:5-7`）：

> DeepSeek Harness（`dsh`）是由 DeepSeek AI 开发的开源 agent harness（智能体框架）。它构建于**一切皆插件**的架构之上，由 [Cordis](https://github.com/cordiverse/cordis) 驱动，其设计参见论文 [_A Programming Paradigm for Spatiotemporal Composability_](https://arxiv.org/abs/2608.25512)。

三个可直接核对的事实：

| 事实 | 出处 |
|---|---|
| 许可证 **MIT** | `README.zh.md:94-96` |
| 状态是**开发者预览**，官方明说"未来将出现破坏兼容性的变更" | `README.zh.md:11-13` |
| 运行只要一条命令：`npx @deepseek-ai/dsh web`，默认在 `http://127.0.0.1:3080` 起 Web UI | `README.zh.md:21-29` |

最后一条值得注意：**它把"跑起来"压成了一条 npm 命令**。这不是好意，这是架构结论——**"一切皆插件"如果成立，那么换一个交付形态就不该需要换一个仓库**（第 54 讲会把这一点推到"一套内核五种形态"）。

**给研究员的第一个抓手**：拿到一个 harness，先跑 `--dump-config`。`dsh` 的这一条是：

```sh
dsh --profile web --dump-config
```

`docs/architecture.zh.md:33-39` 对它的说明是一句判据：

> 它打印出的任何条目，都可以由你自己的 patch 替换。

**"打印出来的都是可替换的"**——这一句话把"可组合"从一个形容词变成了一个**可验证的断言**。

### 2.2 三层：Profile → Bundle → 插件树

`dsh` 的运行体是一棵**插件树**，由启动时按序叠加的各层组合而成（`docs/architecture.zh.md:15-17`）。三层各自的职责，原文给得很清楚：

| 层 | 是什么（原文） | 出处 |
|---|---|---|
| **Profile** | "存放在 Harness home 中的具名组装。它列出自己叠放的组合包，存放自己安装的树外插件，并保存用户自己的 `cordis.patch.yml`" | `docs/architecture.zh.md:19` |
| **Bundle（组合包）** | "是 Cordis 配置项及其挂载代码的分发格式，因此它插入的内容始终可被其上各层 patch" | `docs/architecture.zh.md:21` |
| **插件树** | 各层叠加后的产物；每条 patch"按 id 定位某个条目并替换其整个 config，或插入新条目" | `docs/architecture.zh.md:27` |

随发行版交付的 profile 模板有**五个**：`web`、`headless`、`sdk`、`sdk-minimal`、`acp`（`docs/architecture.zh.md:19`）。

**层与层的声明方式**是同一条约定（`docs/architecture.zh.md:23`）：

> 两者都在各自的 `package.json` 中通过 `dsh` 字段声明自己：`dsh.profile` 列出一个 profile 的组合包，`dsh.bundle` 指向一个组合包的 patch 文件。

**★ 应用顺序是本讲最该记住的一条**（`docs/architecture.zh.md:27`）：

> 各层按此顺序应用在空条目列表之上：先按 profile 列出的顺序应用每个组合包，然后是 profile 的 `cordis.patch.yml`，然后是 home 级的那份，最后是任意 `--patch` overlay。

翻译成工程语言，这是一条 **"覆盖顺序由外到内、由粗到细"** 的规则：

```
空条目列表
  ← 各 bundle（按 profile 列出的顺序）
  ← profile 的 patch         （这一份组装自己的偏好）
  ← home 的 patch            （这一台机器的偏好）
  ← --patch overlay          （这一次运行的偏好）
```

**为什么这个顺序重要**：它让"改一次行为"有四级粒度可选，而且**从最外层的临时覆盖到最内层的发行版默认值，越靠后越强**。你调一个参数做实验，用 `--patch`；这台机器上永远要这么配，用 home patch；这个组装形态一直这么配，用 profile patch；改所有人的默认，才去改 bundle。**四级粒度把"实验"和"发布"分开了**——这正是模型侧做实验时最缺的东西：**同一条实验命令可以完整复现，而不必"记得当时改了哪个文件"。**

### 2.3 `dsh-base`：被共享的第一层，和那个刻意的例外

五个 profile 并不是五套东西。它们共享同一层：**`dsh-base`**（`docs/architecture.zh.md:25`）。

> `dsh-base` 是 `web`、`headless`、`sdk` 与 `acp` profile 的共享第一层：模型适配器、工具、持久化、沙箱与审批策略、设置、凭据、遥测。

注意这一层的内容：**模型接入、工具、持久化、沙箱与审批**——恰好就是你在 43–48 讲逐讲读过的那几个机制。`dsh` 把它们打包成"共享第一层"，于是四个形态**共享同一套模型连接、同一套工具集、同一套持久会话历史、同一套安全默认值**（`packages/bundle/base/README.zh.md:12` 原文）。

**但有一个刻意的例外**（`docs/architecture.zh.md:25` 末句）：

> `dsh-sdk-minimal` 是刻意保留的例外：一个组合包拥有完整的显式 SDK 配置树，不应用 `dsh-base`。

它的自我说明（`packages/bundle/sdk-minimal/README.zh.md:12`）说得更绝——它"**刻意排除** `dsh-base`、Web、settings、托管凭据、遥测、压缩（compaction）、文件系统工具、workspace 指令、skill（技能）、jobs 与 subagent"。

**★ 判据句（本讲的第一个架构判据）**：

> **"共享第一层"是默认，但默认不是铁律。** 当某个交付形态的**约束与共享层相反**时（SDK 客户端要的是"小、显式、可控"），正确做法是**为它单独定义一棵树**，而不是往共享层里塞开关，也不是 fork 出一个仓库。**"一个例外"和"一堆开关"的区别是：例外自己就是完整的、可 `--dump-config` 打印出来的树；开关藏起来的分支不是。**

### 2.4 把六大机制摊在坐标上：本模块后面六讲的位置

到这里，"兑现率可设计"还只是一个说法。把它落成可核对的东西，就是 `dsh` 里的**核心包 → `ctx` 键**这张表（`docs/architecture.zh.md:61-70` 节选）：

| 包 | 职责 | `ctx` 键 |
|---|---|---|
| `core/session` | 仅追加的 `SessionEvent` 日志和内存存储 | `ctx.sessions` |
| `core/system-prompt` | 提示词片段与工具 schema 的组装 | `ctx.systemPrompt` |
| `core/tools` | 作用域化的工具注册表和带把关的执行流水线 | `ctx.tools` |
| `core/agent` | `Agent` 接口、活跃 agent 注册表和 `agent/*` 事件 | `ctx.agents` |
| `core/agent-loop` | 实现该接口的默认驱动器 | `ctx.agentLoop` |
| `llm/llm` | 消息与流式词汇表，以及适配器 seam | `ctx.llm` |

这张表的价值在于：**它把"六大机制"从抽象名词变成了有名字、有位置的对象。** 后面六讲就沿着它走：

| 讲 | 主题 | 在这张表上落点 |
|---|---|---|
| **50** | 一切皆插件：Cordis 的三个动作 | 这些 `ctx` 键**是怎么被注册出来的** |
| **51** | 会话日志：唯一真相源 | `ctx.sessions` |
| **52** | Agent Loop：step / turn 与 waterfall | `ctx.agentLoop` + `agent/*` 事件 |
| **53** | Capability Seam：三个角色 | 上表"seam"那一列的判断依据 |
| **54** | 一套内核，五种形态 | 整棵树怎么被交付成五种产品 |
| **55** | 生态与最佳实践：怎么读一个 harness | 三个抓手 + 工程纪律 |

### 2.5 前沿深挖：从"配置即架构"到"可组合性的代价"

这一讲要留下一条**研究员最容易问错方向**的认知。

先看 `dsh` 这棵树**能做什么**：`docs/architecture.zh.md:11-13` 原文——

> 产品的每一部分都是插件，包括模型适配器、工具注册表、会话日志，以及 agent loop（智能体循环）本身，因此每个都可以从配置替换。不存在需要打补丁的特权内核：扩展 dsh 的方式是把插件挂载到其他插件旁边，而各项注册都是副作用，会在其插件卸载时撤销。

请把这句话里的三个"包括"读慢一点：**模型适配器**（换模型）、**工具注册表**（换工具集）、**`agent loop` 本身**（换那个 while 循环）。第三个是最激进的——**很多系统里 agent loop 是整个架构的"地心"，而这里它是可替换的。**

**但"可组合"不是白拿的。** 代价藏在同两句话的后半段里：**"各项注册都是副作用，会在其插件卸载时撤销"**。

这条约束有两个方向都会咬人：

**方向一：注册不可回收 ⇒ 卸载会留残留。** 如果你绕过框架的注册 API 自己管资源（直接 `addEventListener`、自己开一个全局连接），那么插件卸载时**框架不会替你撤销**——残留就是"看起来卸载了、其实还在"。`dsh` 的教程把这条写成硬规则（`docs/cordis-tutorial/02-lifecycle-and-effects.zh.md:5`）：

> 通过 Cordis API 建立的注册属于 effect，会在所属插件卸载时撤销；**在这些 API 之外管理的资源必须包装在 `ctx.effect()` 中。**

**方向二：依赖不存在 ⇒ 静默等待，不报错。** 这条更隐蔽。插件声明它依赖某个服务（`inject`），如果那个服务**没人提供**，它**不会崩、不会警告**，而是**一直 `PENDING`**（`docs/cordis-tutorial/06-composition-and-hmr.zh.md:42`）：

> 如果插件的 `inject` 指定了无人提供的服务，它就会一直等待，不输出任何内容。这不是错误，因为 PENDING 是合法状态，提供方可能稍后才挂载。

**这是可组合性最典型的失效面：一个"合法状态"和一个"配错了"在观测上长得一模一样。** 而且 `inject` 不是一次性的启动检查——运行期所需服务消失时，依赖它的插件会**整体卸载**；服务恢复后再加载（`docs/cordis-tutorial/03-services.zh.md:76`）。这对"热插拔"是优点，对"我为什么什么都没输出"是灾难。

**★ 判据句（本讲的第二个架构判据，也是模块八的总纲）**：

> **可组合性把"装配期错误"翻译成了"运行期静默状态"。** 装错一个插件的后果，不再是"启动失败"，而是"它不工作，且不告诉你"。因此一个可组合系统**必须额外造出两样东西**：① **能打印出"实际装配了什么"的出口**（`--dump-config` 就是它）；② **把"没输出"和"没配"分开的观测**（否则你永远在 debug 一个根本没错的插件）。
>
> **这两样东西不是可选的附属品，它们是"可组合"这个选择的必要成本。** 看到"某框架什么都能换"，先问它这两件事做没做——**做没做，决定了你能不能在 40 分钟内定位一次装配错误，还是耗掉两天。**

### 2.6 装配的三层与四级覆盖顺序：先画出来

2.2 节给的是顺序规则的原文，2.5 节给的是它的代价。这一节把两者压成一张能带走的图——**从一张空条目列表出发，一共叠几层、谁压得住谁、从哪里能把它打印出来。**

<img class="mermaid-svg" src="/zh/book-assets/diag-0110.svg" alt="2.2 节给的是顺序规则的原文，2.5 节给的是它的代价。这一节把两者压成一张能带走的图——从一张空条目列表出发，一共叠几层、谁压得住谁、从哪里能把它打印出来。" />

> **图 49-1**　装配从空条目列表开始叠四层：bundles（按 profile 列出的顺序）→ profile 的 `cordis.patch.yml` → home 的 `cordis.patch.yml` → `--patch` overlay，**越靠后越强**，最后得到插件树；`--dump-config` 是"打印最终树的出口"。

这张图里有三个可以直接核对的事实，都不是修辞：

1. **这个顺序是明文写死的，不是"约定"。** `apps/cli/src/dump-config.ts:51-75` 的 `collectConfigDumpLayers()` 就是它的源码实现——函数体按 bundle → profile → home → `--patch` 依次 `layers.push`（`apps/cli/src/dump-config.ts:51-75`）。
2. **打印出来的树，和真正启动时装的是同一份合并算法。** `renderConfigDump()` 的 JSDoc 明说它把每个 layer 的 patch 作为"ONE flattened list"交给 `applyEntryPatches`，并强调这是 "the same call `boot()` makes"（`packages/boot/app-boot/src/index.ts:398-407`）。**"dump 和 boot 走同一条路"是这条断言能成立的前提**；两者一旦分叉，打印出来的树就只是文档，不是事实。
3. **还有一条"少一层"的变体。** `userLayer: false` 会跳过读 `cordis.patch.yml`，因此只打 bundles 层（`packages/boot/app-boot/src/profile.ts:698-700`）——这就是 `--dump-default-config`。

**代码级细节：这条链路的两端都能读。** 起点是 commander 注册的三个互斥选项：

```ts
    .option('--dump-config', 'print the composed profile tree and exit')
    .option('--dump-config-schema', 'print JSON Schema for profile entries and patches without mounting')
    .option('--dump-default-config', 'print the profile tree without its user layer or --patch overlays and exit')
```

（`apps/cli/src/args.ts:170-172`）

中间那一跳把"层"变成"带来源的若干段 YAML"，由 `groupedDump()` 完成——它给每一段打上来源注释：

```ts
  const flush = (): void => {
    if (currentLabel === undefined || group.length === 0) return
    lines.push(`# == ${currentLabel}`)
```

（`packages/boot/app-boot/src/index.ts:503-508`）

所以 `--dump-config` 的输出不只是"有哪些条目"，而是"**每一段条目来自哪个文件**"。调试装配错误时，这一条比条目清单本身更值钱——**它能直接回答"我改的那一行到底生效了没有"。**

### 2.7 核心包到 `ctx` 键的地图，与两条失效链

2.4 节那张表是"静态坐标"。把它连成图，才能看出后面六讲各自打开的是哪一格。

<img class="mermaid-svg" src="/zh/book-assets/diag-0111.svg" alt="2.4 节那张表是&quot;静态坐标&quot;。把它连成图，才能看出后面六讲各自打开的是哪一格。" />

> **图 49-2**　六个核心包各占住一个 `ctx` 键（左→中），本模块 50–55 讲分别打开其中一块（右）。中间那一列是可替换的实际抓手：**消费方只认 key，从不 import 实现**。

图右侧"哪一讲主要打开它"是按 2.4 节落点表做的归类（**这一句归类是作者的推论，不是原文**）；图左与图中两列则是 `docs/architecture.zh.md:61-70` 那张表逐行逐字的搬运。

**然后是这张地图的反面：两条失效链。** 可组合性的代价不会以"报错"的形式出现，它以两条静默路径出现——把它们画出来，才知道要盯哪里。

<img class="mermaid-svg" src="/zh/book-assets/diag-0112.svg" alt="然后是这张地图的反面：两条失效链。 可组合性的代价不会以&quot;报错&quot;的形式出现，它以两条静默路径出现——把它们画出来，才知道要盯哪里。" />

> **图 49-3**　可组合性的两条失效链：① 注册不可回收 ⇒ 卸载留残留（必须走 `ctx.effect()`）；② `inject` 无提供方 ⇒ 停在 PENDING 静默等待、不报错。两条链的终点相同：**观测上，合法状态与配错了长得一模一样。**

第二条链在 `dsh` 里有一整段**可读的诊断实现**。启动结算时，它对每个非 ACTIVE 的 fiber 按 `fiber.inject` 逐项检查 `fiber.ctx.get(service) === undefined`，把缺失的服务名收集起来：

```ts
    if (state === FIBER_PENDING) {
      const missing = Object.keys(fiber.inject).filter(service => fiber.ctx.get(service) === undefined)
      failures.push({
        entry,
        outcome: { kind: 'pending', missing },
      })
    }
```

（`packages/boot/app-boot/src/index.ts:839-858`）

`missing` 最终被渲染成一行人能读的文案——**这正是 2.5 节说"必须额外造出的第二样东西"**：

```ts
function inactiveDiagnostic({ entry, outcome }: InactiveEntry): string {
  const detail = outcome.kind === 'failed' ? failureDetail(outcome)
    : `pending (waiting for ${outcome.missing.length === 1 ? 'service' : 'services'}: ${outcome.missing.join(', ') || 'unknown'})`
  return `${entry.options.id} (${entry.options.name}): ${detail}`
}
```

（`packages/boot/app-boot/src/index.ts:879-883`）

并且它会把这些条目单独列成一段：

```ts
  if (pending.length > 0) {
    const width = Math.max('Plugin'.length, ...pending.map(({ entry }) => label(entry).length)) + 2
    lines.push('', `Plugins waiting for services (${String(pending.length)}):`, `  ${'Plugin'.padEnd(width)}Missing services`)
```

（`packages/boot/app-boot/src/index.ts:899-905`）

**注意这三段代码的分工**：第一段是"把 PENDING 算出来"，第二段是"把它翻译成人话"，第三段是"把它从日志里拎出来单独成段"。**一个可组合系统要能 debug，这三段缺一不可**——只算出状态没翻译，你看到的是数字；只翻译不单列，它会被淹在启动日志里。

还有一处细节值得记：这个"PENDING 判定"在 harness 侧用的是**运行时镜像常量**，因为 Cordis 的 `FiberState` 是 `const enum`、没有可导入的运行时对象：

```ts
/**
 * Value mirrors used because Cordis's const enum has no runtime object to import.
 * Keep aligned with `packages/client/web/src/loader-status.ts`.
 */
const FIBER_PENDING = 0 as FiberState.PENDING
```

（`packages/boot/app-boot/src/index.ts:730-734`）

**这条注释本身就是一个可迁移的教训**：当你把一个枚举当"协议"用过边界时，要么导出运行时对象，要么显式维护一份镜像并写明"保持对齐"——否则枚举改一个顺序，所有镜像点静默错位。

### 2.8 一手数据：这一层有多少件东西

本节的数全部是可机械复算的规模量；每个数后面给出复算方式。

| 数 | 值 | 复算方式 / 出处 |
|---|---|---|
| TypeScript 源文件 | **3,643** 个 | `find packages -name '*.ts' -not -name '*.d.ts'`（`_build/m8-facts/SPEC-加深度规范.md` 第 6 节） |
| TypeScript 总行数 | **783,651** 行 | 同上命令加 `wc -l` |
| 一级包组 | **54** 个 | `ls -d packages/*/` |
| bundle 目录 | **6** 个 | `_build/m8-facts/SPEC-加深度规范.md` 第 6 节 |
| profile 模板 | **5** 个 | `packages/boot/app-boot/src/profile.ts:178-195` 的 `PROFILE_TEMPLATES` 逐键计数 |
| `apps/` 下应用目录 | **4** 个 | `ls -d apps/*/ \| wc -l`（`_build/m8-facts/cordis-core.md` 第 8 节） |
| `vendor/` 下一级兄弟目录 | **9** 个 | `find vendor -maxdepth 2 -type d`（`_build/m8-facts/cordis-core.md` 第 1.1 节） |
| 核心包 → `ctx` 键表的行数 | **8** 行数据 | `docs/architecture.zh.md:61-70`（含表头分隔共 10 行，数据行 8 行） |
| 装配覆盖层数 | **4** 层 | `docs/architecture.zh.md:27` 逐项列举：bundle / profile patch / home patch / `--patch` |

最后一行把本讲的"四级粒度"从一个形容词变成了一个可数的数：**`docs/architecture.zh.md:27` 那一句列举里正好四项**，没有第五项。

再补一个几乎不用数的对照——`PROFILE_TEMPLATES` 的五个键里，有**四个**共用 `dsh-base` 作为第一层（`acp` / `web` / `headless` / `sdk` 的 `bundles` 数组首项都是 `@deepseek-ai/dsh-base`），只有 `sdk-minimal` 是单元素数组 `['@deepseek-ai/dsh-sdk-minimal']`（`packages/boot/app-boot/src/profile.ts:178-195` 逐行）。**2.3 节说的"那个刻意的例外"，在这段源码里就是一行单元素数组。**

### 2.9 搬到你自己系统：给你的系统做一次"可打印的装配"

`dsh` 这条链最有迁移价值的不是"有 profile"，而是"**装配过程本身是一个可打印、可复算的纯函数**"。把它搬回你自己的系统，问的是下面这些问题：

| `dsh` 的做法 | 你自己系统里"该问的问题" |
|---|---|
| 空条目列表 + 四层 patch 依次叠加 | 我的启动配置是"一份文件"，还是"可叠加的若干层"？ |
| 每层带来源标签（`# ==` 注释） | 我能不能说出每条生效配置来自哪个文件？ |
| `--dump-config` 打印最终条目列表 | 我有一条命令能打印"这次实际装配了什么"吗？ |
| `--dump-default-config` 只打 bundles 层 | 我区分得开"发行版默认值"和"我现在跑的值"吗？ |
| patch 按 id 定位、**整段替换** config | 我的覆盖是"按 key 深合并"，还是"整段替换"？后者才可复现 |
| dump 与 boot 共用 `applyEntryPatches` | 我的"打印配置"和"真正加载"是同一份代码吗？ |

**可执行的落地步骤（4 步）**：

1. **定义层与层序**：写死一个层顺序常量（默认值 → 环境 → 用户 → 本次运行），并强制每层携带一个来源标签（文件路径或参数名）。层序必须是代码里的一个数组，不是散落在 if-else 里的判断。
2. **实现 `dump` 入口**：按第 1 步的层序合并，然后**按来源分组打印**（借 `# ==` 那种注释形态）。硬要求：**合并调用与启动时同一份函数**，不允许复制一份。
3. **补"未激活条目"诊断**：启动末尾列出"声明了但没生效"的条目及原因（缺哪个依赖、缺被谁提供）。这就是 2.7 节那三段代码的最小可迁移版本。
4. **接进 CI 快照**：把 dump 输出存成快照；任何人改层序或 patch，diff 里必须能看出"哪些条目变了、来自哪一层"。

**★ 判据句**：

> **一个系统能不能被"可打印地组装"，判据只有一条：有没有一条命令，能在不启动业务的前提下，输出"这次实际装了哪些条目、每条来自哪一层"。** 打印不出来，你的"可组合"就不可验证；打印出来但与真实加载不同源，你的"可组合"就不可信。

## 三、避坑清单

- [ ] **别把"表现差"自动归因到模型。** 先问：上文里模型实际看到了什么、工具 schema 是谁写的、失败是谁拦的——这三件都不在模型里。
- [ ] **做模型侧实验时，把 harness 当受控变量写进方法。** 没交代 harness 的"提升 X%"，无法排除"其实是把工具描述写清楚了"。
- [ ] **拿到一个可组合系统，第一件事是让它把装配树打印出来**（`dsh --profile <name> --dump-config`）。打印不出来 ⇒ 它的"可组合"不可验证。
- [ ] **改配置按四级粒度选**：一次实验用 `--patch`，一台机器用 home patch，一种形态用 profile patch，所有人默认才改 bundle。别一上来就改 bundle。
- [ ] **别把"要一个小形态"做成"往共享层加开关"。** 与共享层约束相反时，单独定义一棵显式的树（`sdk-minimal` 的做法），而不是加第 N 个布尔开关。
- [ ] **自己管的资源必须包成 effect。** 绕过注册 API 的副作用，卸载时不会有人替你撤销——这就是"假卸载"。
- [ ] **遇到"插件没输出"，先怀疑 `inject` 依赖没人提供**（合法 `PENDING`），而不是先怀疑逻辑写错。
- [ ] **不要用"这一版的事实"当永久契约。** `dsh` 是开发者预览，官方预告破坏性变更；引用路径要带版本（本模块统一用 commit `477b4f4`）。
- [ ] **别把 `--dump-config` 的输出当成"运行状态"。** 它打印的是**装配结果**（条目清单）；某条是 PENDING 还是 ACTIVE，要看启动诊断里那段 `Plugins waiting for services`（`packages/boot/app-boot/src/index.ts:899-905`）。"树里有这条" ≠ "这条在工作"。
- [ ] **别把 `--dump-default-config` 当 `--dump-config` 用。** 前者刻意跳过 profile 的 user layer 与 `--patch`，是"发行版默认值"的恢复诊断（`packages/boot/app-boot/src/profile.ts:698-700`），不是"我现在实际跑什么"。

## 四、动手任务

> **目标**：亲手确认"兑现率可设计"这句话，不是修辞。

**步骤 1：跑起来，然后打印装配树**

先跑起来：

```sh
npx @deepseek-ai/dsh web        # 默认 http://127.0.0.1:3080
```

再打印这棵树：

```sh
npx @deepseek-ai/dsh --profile web --dump-config
```

**任务**：数一数树里有多少条目，并从中**找到"模型适配器"和"agent loop"这两个条目**。找到它们，本讲 2.5 节那句"连 agent loop 本身都是插件"就从传言变成了你亲眼看到的一行。

**步骤 2：找一次"替换"的最短路径**

在打印出的条目里挑一个（比如模型适配器），**不要改 bundle**，用 `--patch` 覆盖它一次。**任务**：验证"四级粒度"里最外层那级真的能生效，且**不需要重新构建**。

**步骤 3：亲手制造一次"静默 PENDING"**

按 `docs/cordis-tutorial/01-first-plugin.zh.md:12-19` 抄一个最小插件（整段只有 7 行）：

```ts
import type { Context } from '@deepseek-ai/cordis'

export const name = 'hello'

export function apply(ctx: Context) {
  console.log('hello from my first plugin')
}
```

先让它在**没人提供依赖**的情况下声明一个 `inject`，观察它是不是"一直不输出、也不报错"。**任务**：写出你用来区分"静默等待"和"真的挂了"的那条观测手段——**如果你写不出来，说明你还没理解可组合性的代价在哪。**

**步骤 4：把这张坐标搬回你自己的系统**

用 2.4 节那张表的四列（**包 / 职责 / `ctx` 键 / 谁消费**），给你自己的系统列一份。**验收标准**：你的"agent loop"那一行，能不能被单独替换掉？如果答案是"不能，它散在 5 个类里"，那正是这一模块后面要补的洞。

**步骤 5：把"装配结果"和"运行状态"分成两列看**

先拿装配结果：

```sh
npx @deepseek-ai/dsh --profile web --dump-config
```

再从启动日志里找出 `Plugins waiting for services` 那一段（对应 `packages/boot/app-boot/src/index.ts:899-905` 的输出形态）。

**任务**：填一张两列对照表——左列"树里有的条目"，右列"实际 ACTIVE 了吗"。确认这两列**不相等**，并逐条写出差额的原因属于哪一类：① 依赖服务没人提供（合法 `PENDING`）；② 条目被上层 patch 改坏（`FAILED`）；③ 其它。**这一步做完，你就亲手把 2.5 节那句"装配期错误被翻译成运行期静默状态"变成了自己的两张表。**

---

## 本讲小结

1. **模型给上限，harness 给兑现率。** "模型不行"是不能证伪、也不能改进的归因；换 harness 差一个档，差的是兑现率。
2. **`dsh` 用三层组织兑现率**：`Profile`（具名组装）→ `Bundle`（可被上层 patch 的分发格式）→ 插件树（启动时按序叠加）。
3. **应用顺序由粗到细**：bundle → profile patch → home patch → `--patch`。四级粒度把"实验"与"发布"分开，让一次实验可完整复现。
4. **共享第一层是默认，默认不是铁律。** 与共享层约束相反时，单独定义一棵显式的树（`sdk-minimal`），而不是往共享层堆开关。
5. **"打印出来的都是可替换的"** —— `--dump-config` 把"可组合"从形容词变成可验证的断言。
6. **可组合性的代价是"装配期错误被翻译成运行期静默状态"**：注册不可回收 ⇒ 假卸载；依赖没人提供 ⇒ 合法 `PENDING` 却没输出。所以可组合系统必须自带"打印实际装配"的出口和"区分没输出与没配"的观测。
7. **对研究员最实际的一条**：harness 是你实验里的受控变量，也是你不改权重就能拿回性能的杠杆。

下一讲我们打开"一切皆插件"的底层框架：**Cordis**。它的插件只做三件事——贡献**服务**、发**类型化事件**、装**可逆副作用**。我们要回答的核心问题是：**这"三个动作"凭什么能撑起一个产品？它的账单又记在哪一笔上？**
