# 本地开发环境变量模板
# 用法：复制为 .env 并填入真实值（.env 已被 .gitignore 忽略，不会提交）
#   cp .env.example .env
GITEA_API_TOKEN=
GITEA_WEBHOOK_SECRET=
TOKENHUB_API_KEY=
# API 访问令牌：保护 /api/** 与 /api/admin/**（Bearer 校验）。生产务必设置强随机值，留空则零鉴权。
REVIEW_API_TOKEN=
